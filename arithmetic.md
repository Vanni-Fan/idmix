# XID v1.1 规范：自描述整数序列短标识符编码

## 1. 概述

XID 是一种紧凑、自描述的二进制到文本编码方案，专为 **只包含简单整数**（小值居多，负数偶尔出现且一般 ≥ -10）的数据序列设计。它将多个带类型的整数压缩为极短的字节序列，再通过**自定义字符表**转换为字符串，可作为 access_key、短 ID、令牌等使用。

**核心特性**：

- **类型自描述**：每个整数携带原始类型（uint8/int64 等），解码时不依赖外部 schema。
- **极致压缩**：[0,-15] 的正数、[-15,-1] 的负数仅占 **1 字节**；其他整数去除冗余符号字节后按最小宽度存储。
- **32 态多态**：同一组数据可生成 32 种不同字符串，方便防猜、过滤敏感词。
- **轻量自校验**：内嵌 2-bit 校验，可即时阻挡 75% 的随机篡改或传输错误，不增加额外字节。
- **私密进制编码**：最终输出并非标准 Base64，而是采用**自定义字符集（64 字符）** 进行 Base64 无填充编码，字符顺序可作为密钥。
- **高性能**：纯二进制位操作及异或混淆，编解码极快，无需 AES 等重量级加密。

**适用场景**：

- 需在 URL、Cookie、日志中携带多个整数信息的 access_key。
- 类似 Sqids 的短 ID 生成，但要求携带更多结构化信息。
- 对防猜有一定要求，但不需要强加密的内部系统。

**限制**：

- 单次编码最多支持 **511 个**数据对象（可通过配置调整）。
- 数值范围理论上支持 uint64/int64 全范围，但推荐用于中小整数，过大的值会降低压缩率。

---



## 2. 二进制格式

XID 的二进制块由 **整体头（2 字节）** + **数据对象序列** 组成。

### 2.1 整体头（16 位，小端序）

将两个字节视为一个 16 位无符号整数 `header = data[0] | (data[1] << 8)`，其位分配如下（默认配置）：


| 位域         | 宽度  | 含义                       |
| ---------- | --- | ------------------------ |
| bit[1:0]   | 2   | `check` 校验位              |
| bit[10:2]  | 9   | `count` 数据对象个数（0~511）    |
| bit[15:11] | 5   | `variant_id` 变体 ID（0~31） |


- `check`：初始为 0，编码时填入全局异或校验和的低 2 位。
- `count`：后续数据对象的数量。
- `variant_id`：控制多态性，32 种变体。

**字节序**：小端，即低地址存放低 8 位（`byte0 = header & 0xFF`），高地址存放高 8 位（`byte1 = (header >> 8) & 0xFF`）。

### 2.2 数据对象

每个数据对象编码一个整数及其原始类型，由 **1 字节对象头** + **可选数据负载** 构成。对象头根据最高位分为两种模式。

#### 模式 A：内嵌模式（bit7 = 0）

对象总长度 = **1 字节**。  
对象头位布局：

```
bit7   = 0
bit6   = sign       (0: 非负, 1: 负)
bit5-4 = width_bits (原始位宽: 00=8位, 01=16位, 10=32位, 11=64位)
bit3-0 = value      (内嵌值,存绝对值,4位范围为 0~15,所以 [-15,15] 的值都可以内嵌存)
```



#### 模式 B：扩展模式（bit7 = 1）

对象总长度 = 1 + **存储宽度字节数**。  
对象头位布局：

```
bit7   = 1
bit6   = sign       (0: 原值为非负, 1: 原值为负)
bit5-4 = sw         (存储宽度: 00=1字节, 01=2字节, 10=4字节, 11=8字节)
bit3-0 = otype      (原始类型索引, 0~7)
```

后紧跟 `sw` 字节的数据负载，按**无符号小端**存储**正数表示** `P`（见下文）。

**原始类型索引** `otype` **映射**：


| otype | 原始类型   |
| ----- | ------ |
| 0     | uint8  |
| 1     | uint16 |
| 2     | uint32 |
| 3     | uint64 |
| 4     | int8   |
| 5     | int16  |
| 6     | int32  |
| 7     | int64  |


**存储宽度** `sw` **对应字节数**：


| sw     | 字节数 |
| ------ | --- |
| 0 (00) | 1   |
| 1 (01) | 2   |
| 2 (10) | 4   |
| 3 (11) | 8   |


**数据负载生成规则（正数表示 + 最小字宽）**

因 `otype` 已记录原始类型，解码时不存在符号歧义。编码步骤：

**1. 正数表示** `P` **与符号位** `sign`

- 若原值 `V < 0`：`P = ~V + 1`（补码取反加一，等价于 `|V|`）；`sign = 1`。
- 若原值 `V ≥ 0`：`P = V`；`sign = 0`。
- 无符号类型（otype 0~3）恒有 `V ≥ 0`，故恒 `sign = 0`。

**2. 存储宽度** `sw`**（仅由** `P` **的大小决定，与 otype 位宽无关）**


| 条件               | sw  | 字节数 |
| ---------------- | --- | --- |
| `P < 256`        | 0   | 1   |
| `P < 65536`      | 1   | 2   |
| `P < 4294967296` | 2   | 4   |
| 否则               | 3   | 8   |


负载 = `P` 的无符号小端整数，宽度取上表字节数。**不要求**负载最高字节 bit7 为 0。

**3. 解码**

1. 按 `sw` 读出无符号小端整数 `P`。
2. 读对象头 `sign`（模式 B 的 bit6，或模式 A 的 bit6）。
3. 若 `sign = 1`：还原 `V = ~P + 1`（即 `-P`）。
4. 若 `sign = 0`：`V = P`。
5. 按 `otype` 校验 `V` 落在声明类型范围内。

**示例**：`int64(-1)` → `P=1`, sw=0, 负载 `0x01`；`uint32(40)` → `P=40`, 负载 `0x28`；`uint64` 最大值 → sw=3, 8 字节负载。

编码器优先选用模式 A（当 `P < 16` 且满足内嵌表），否则选用上表最小 `sw`。

---



## 3. 多态性与混淆

使用 `variant_id` 对数据对象部分的**所有字节**（包括每个对象的头字节和负载字节）进行可逆异或混淆，使得同一组数据在不同 `variant_id` 下产生不同的二进制序列。

- 混淆掩码计算：`mask = ((variant_id * 0x9D) + 0x37) & 0xFF`
- **编码时**：对数据对象区域的每个字节执行 `byte ^= mask`
- **解码时**：读取 `variant_id`，计算相同 `mask`，再次异或即可还原。

整体头（2 字节）**不参与混淆**，以保证解码器能直接提取 `count` 和 `variant_id`。

---



## 4. 自校验

校验覆盖**整个二进制块**（整体头 + 混淆后的数据对象）。

**编码时**：

1. 将整体头中 check 位（bit1-0）暂置为 0。
2. 拼接整体头及混淆后的数据对象所有字节，计算它们的**异或和**（XOR）。
3. 取异或和的低 2 位，写入整体头的 check 位。

**解码时**：

1. 保存接收到的 `check` 值。
2. 将整体头中的 check 位清零（其他位不变），得到临时二进制块。
3. 计算临时二进制块全部字节的异或和，取低 2 位 `calc_check`。
4. 若 `calc_check != check`，则判定数据无效，拒绝解码。

此 2 位校验可阻挡 75% 的随机篡改，且不增加额外字节。

---



## 5. 文本编码（自定义字串的进制编码）

---



## 6. 配置与扩展

- 整体头位域分配可通过编译时宏或配置文件调整，例如：
  - 若不需要 32 态，可将 `variant_id` 降为 3 位，`count` 增至 11 位（最大 2047 对象）。
  - 保留位始终为 0，可作未来扩展。
- 自定义字符表建议在部署时随机生成，保证各服务实例的编码不互通。
- 内嵌模式覆盖 **[-15, 15]**，已覆盖绝大多数小整数；若数据分布变化，可考虑调整内嵌位数（需同步修改规范）。

---



## 7. 对比



### 7.1 编码长度（XID vs MsgPack / CBOR / Protobuf）

以下为本仓库 Go 参考实现实测（variant=0，默认 62 字符表）。二进制格式统一 **base64** 后计字符数；`XID` 列为文本层字符串长度。


| 场景             | XID | XID(b64) | MsgPack | CBOR | Protobuf |
| -------------- | --- | -------- | ------- | ---- | -------- |
| spec_example   | 9   | 12       | 64      | 32   | 36       |
| uint32_max     | 10  | 16       | 24      | 16   | 16       |
| int32_min      | 10  | 16       | 24      | 16   | 20       |
| int64_min      | 16  | 24       | 24      | 24   | 20       |
| int64_max      | 16  | 24       | 24      | 24   | 20       |
| uint64_max     | 16  | 24       | 24      | 12   | 20       |
| mixed_extremes | 54  | 72       | 104     | 80   | 92       |
| access_key     | 16  | 24       | 64      | 40   | 32       |
| embedded_small | 9   | 12       | 84      | 40   | 56       |


`mixed_extremes` = `uint32_max` + `int32_min` + `int64_min` + `int64_max` + `uint64_max` 五字段同块编码。复现：`cd golang && go test -v -run TestCompareSerializationFormats`。

### 7.2 特性对比


| 特性                 | **XID (本算法)**                                                   | **CBOR (RFC 7049)**              | **MessagePack**              | **Protobuf Varint + ZigZag**           |
| ------------------ | --------------------------------------------------------------- | -------------------------------- | ---------------------------- | -------------------------------------- |
| **类型自描述**          | ✅ 每个整数携带原始类型（uint8/int64 等）                                     | ✅ 支持多种数据类型，整数不区分位宽               | ✅ 支持多种类型，整数不区分位宽             | ❌ 完全依赖外部 `.proto` schema               |
| **保留原始整数位宽**       | ✅ 可区分 `int32` 和 `int64`，还原时精确复原                                 | ❌ 所有正整数统一为无符号大整数，负数按 `-1-n` 编码   | ❌ 同 CBOR，不保留具体位宽             | ❌ 需 schema 约定，编码本身不分 `int32` 或 `int64` |
| **小正数压缩[0, 15]**   | 🟢 **1 字节**（内嵌）                                                 | 🟢 0~23 用 1 字节                   | 🟢 0~127 用 1 字节              | 🟢 0~127 经 Varint 仍可能 1 字节             |
| **小负数压缩[-1, -15]** | 🟢 **1 字节**（内嵌）                                                 | 🟢 -1~-24 用 1 字节（实际为 0x20..0x37） | 🟢 -32~-1 用 1 字节（0xe0..0xff） | 🟢 ZigZag 后 Varint 也能做到 1 字节           |
| **中等整数（例：1000）**   | 🟡 2 字节（扩展：1 头 + 1~2 负载）                                        | 🟡 3 字节（0x19 0x03 0xe8）          | 🟡 3 字节（0xcd 0x03 0xe8）      | 🟡 2 字节（Varint 变长）                     |
| **大整数 / 极值**       | 🟢 按正数表示 `P` 选 sw（见 §2.2）；`uint32_max` 文本 10 字符，短于 CBOR/MsgPack | 🟡 1/2/4/8 字节显式标记                | 🟡 类似 CBOR                   | 🟡 Varint；uint64 极值 base64 后 12~24 字符  |
| **多态性（同数据多种输出）**   | ✅ 5-bit `variant_id` 生成 32 种不同编码                                | ❌                                | ❌                            | ❌                                      |
| **内建快速校验**         | ✅ 2-bit 异或校验，阻挡 75% 随机错误，零额外字节                                  | ❌（可加 tag，但需额外空间）                 | ❌                            | ❌                                      |
| **输出文本形式**         | 自定义字符表 Base64（可私密化）                                             | 二进制为主，转文本需额外步骤                   | 二进制为主，转文本需额外步骤               | 二进制为主，转文本需额外步骤                         |
| **抗篡改 / 防猜**       | 🟢 校验+多态+可选私密进制表，无 schema 不易解读                                  | 🔴 无防护，明文二进制                     | 🔴 无防护                       | 🔴 无防护（除非外部加密）                         |
| **解码速度**           | 🟢 极快，位操作 + 简单异或                                                | 🟢 较快，状态机解析                      | 🟢 较快                        | 🟢 极快（Varint 循环少）                      |
| **适用场景**           | 短 access_key、带多字段的防猜短 ID                                        | 通用二进制序列化，IoT                     | 通用二进制序列化，RPC                 | 高性能 RPC，需 schema                       |
| **Schema 依赖**      | 无，完全自解释                                                         | 无                                | 无                            | 必须有 `.proto` 才能解码                      |


**文档结束**  
XID v1.1 规范